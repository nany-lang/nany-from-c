* C-style typedef'ed named structs are not managed properly : check for a typedef on next decl that uses the same recordDecl and merge ?
* There is a bug with the Indenter's post-increment / post-decrement operators. They do the work twice... (currently worked-around by not using them)
* new array creation is broken